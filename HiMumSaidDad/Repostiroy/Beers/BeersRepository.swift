import Foundation

protocol BeersRepository {

    func fetchAllBeers(completion: Result<[Beer], Error>)
}

extension Repoistory {

    func fetchAllBeers(completion: Result<[Beer], Error>) {
        

    }
}
